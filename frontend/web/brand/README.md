# 溯知 · 工程内品牌资产

本目录是 **上线引用副本**。范式与归档说明在笔记库 `departments/lab`（见 `产品LOGO-从笔记对照到工程归档.md`）。**不要把某一台机器的 Obsidian 绝对路径当成唯一地址。**

实验室主标只从官方 `LUXI LAB.svg` 单向拷贝为 `luxi-lab-main.svg`。勿把听默几何 Y+L 当主标。

## 文件

| 文件 | 用途 |
| :--- | :--- |
| `favicon.svg` | 浏览器图标（32 视口） |
| `suzhi-mark.svg` | 512 方标（顶栏 / 关于 / apple-touch） |
| `溯知-产品标识.svg` | 方标中文命名副本（与 mark 同源） |
| `logo.svg` | 横版字锁 |
| `溯知-横版字锁.svg` | 字锁中文命名副本 |
| `luxi-lab-main.svg` | 实验室主标（页脚 / 关于页出品方） |

色板：鹿溪绿 `#0D5E42` · 源启白 `#F5F7FA` · 进化蓝 `#00D2FF` · 标题金 `#F1C40F`。

产品标语义：绿底圆角砖、三信源汇流、成文章页（大纲三行）、清溪自下而上（溯流）、金落点、源启星。

工程静态路径：`frontend/web/brand/`，Web 入口以 `/static/brand/` 引用。
